server:
  port: 8888
  
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: c:/microservice-repo
  sleuth:
    sampler:
      probability: 0.1